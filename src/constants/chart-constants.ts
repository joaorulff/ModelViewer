// LABEL SECTION
export const LABELSECTIONWIDTHRATIO: number = 0.2;
export const LABELSECTIONMARGINS: {top: number, left: number, bottom: number, right: number} = {
    top: 5,
    left: 5,
    right: 5,
    bottom: 30
}

// MATRIX SECTION
export const MATRIXSECTIONWIDTHRATIO: number = 0.6;
export const MATRIXSECTIONMARGINS: {top: number, left: number, bottom: number, right: number} = {
    top: 5,
    left: 5,
    right: 5,
    bottom: 30
}

// SUMMART SECTION
export const SUMMARYSECTIONWIDTHRATIO: number = 0.2;
export const SUMMARYSECTIONMARGINS: {top: number, left: number, bottom: number, right: number} = {
    top: 5,
    left: 5,
    right: 5,
    bottom: 30
}


// TEMPORAL MODEL VIEWER
export const TEMPORALMATRIXSECTIONMARGINS: {top: number, left: number, bottom: number, right: number} = {
    top: 10,
    left: 50,
    right: 10,
    bottom: 30
}


// CONTINUOUS MODEL VIEWER
export const CONTINUOUSMAINSECTIONMARGINS: {top: number, left: number, bottom: number, right: number} = {
    top: 10,
    left: 80,
    right: 10,
    bottom: 30
}
